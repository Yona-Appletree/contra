// The lark, alone: the end-effect proof. `$partner` is provided and is
// nobody, so every move stands and an `if ($partner)` takes its else.

module solo() {
  place lark role Lark at back(0.4m);
  provide $partner: Place = other(me);
  provide $role: Role = role(me);
}
