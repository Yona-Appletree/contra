// Every .dance file is read after this one.

enum Role { Lark, Robin }
enum Hand { Left, Right }
enum Turn { Left, Right }
enum Axis { X, Y }
enum Direction { Up, Down, Left, Right }
enum Phrase { A1, A2, B1, B2 }
